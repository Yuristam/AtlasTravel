@model AtlasTravel.MVC.ViewModels.AdminDashboardViewModel

@{
    ViewData["Title"] = "Admin Dashboard";
}

<h2 class="mb-4">Панель администратора</h2>

<div class="row">

    <!-- Карточка пользователей -->
    <div class="col-md-3">
        <div class="card text-white bg-primary mb-3">
            <div class="card-body">
                <h5 class="card-title">Пользователи</h5>
                <p class="card-text fs-4">@Model.TotalUsers</p>
                <a asp-controller="Admin" asp-action="Users" class="text-white">Подробнее</a>
            </div>
        </div>
    </div>

    @* <!-- Карточка поездок -->
    <div class="col-md-3">
        <div class="card text-white bg-success mb-3">
            <div class="card-body">
                <h5 class="card-title">Поездки</h5>
                <p class="card-text fs-4">@Model.TotalTrips</p>
                <a asp-controller="Admin" asp-action="Trips" class="text-white">Подробнее</a>
            </div>
        </div>
    </div> *@
@* 
    <!-- Карточка сообщений -->
    <div class="col-md-3">
        <div class="card text-white bg-warning mb-3">
            <div class="card-body">
                <h5 class="card-title">Отзывы</h5>
                <p class="card-text fs-4">@Model.TotalFeedbacks</p>
                <a asp-controller="Admin" asp-action="Feedbacks" class="text-white">Подробнее</a>
            </div>
        </div>
    </div>
 *@
</div>

<!-- Пример вставки графика через iframe, React или JS -->
<div class="mt-5">
    <h4>Рост пользователей</h4>
    <div id="user-growth-chart">
        <!-- Здесь можно вставить Recharts график через React, iframe или Chart.js -->
        <img src="/images/sample-chart.png" alt="User growth" class="img-fluid" />
    </div>
</div>



@* @{
    ViewData["Title"] = "Admin Dashboard";
}

<h1 class="display-4">Admin Dashboard</h1>

<p>Welcome to the admin dashboard. Here you can manage users and view statistics.</p>

<div class="row">
    <div class="col-md-6">
        <h2>Users</h2>
        <p>Total Users: @Model.TotalUsers</p>
        <p>Active Users: @Model.ActiveUsers</p>
        <p>Inactive Users: @Model.InactiveUsers</p>
        <a class="btn btn-primary" asp-action="Users">Manage Users</a>
    </div>
    <div class="col-md-6">
        <h2>Statistics</h2>
        <p>Total Bookings: @Model.TotalBookings</p>
        <p>Total Revenue: @Model.TotalRevenue.ToString("C")</p>
    </div>
</div>

<div class="mt-3">
    <a class="btn btn-info" asp-action="Index" asp-controller="Home">Back to Home</a>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
} *@
<!-- This is the Admin Dashboard view for the AtlasTravel MVC application. It displays user statistics and allows management of users. -->